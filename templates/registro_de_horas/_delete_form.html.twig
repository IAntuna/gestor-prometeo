<form method="post" action="{{ path('app_registro_de_horas_delete', {'id': registro_de_hora.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ registro_de_hora.id) }}">
    <button class="btn">Delete</button>
</form>
