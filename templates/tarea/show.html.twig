{% extends 'base.html.twig' %}

{% block title %}Detalle de la Tarea{% endblock %}

{% block body %}
    <h1 class="mb-4">Detalle de la Tarea</h1>

    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tarea.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tarea.nombre }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ tarea.descripcion }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ tarea.estado }}</td>
            </tr>
            <tr>
                <th>Plazo</th>
                <td>{{ tarea.plazo ? tarea.plazo|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Horas Estimadas</th>
                <td>{{ tarea.horasEstimadas }}</td>
            </tr>
            <tr>
                <th>Horas Realizadas</th>
                <td>{{ tarea.horasRealizadas }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tarea_index') }}" class="btn btn-secondary mt-3">Volver a la lista</a>
    <a href="{{ path('app_tarea_edit', {'id': tarea.id}) }}" class="btn btn-primary mt-3">Editar</a>

    {{ include('tarea/_delete_form.html.twig') }}
{% endblock %}
